import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';

export default function ProjectionChart({principal,rate,months=12}:{principal:number;rate:number;months?:number}){
  const data=Array.from({length:months+1},(_,month)=>{const expected=principal*(1+rate/100*month/12);return {month:`M${month}`,low:Math.round(principal+(expected-principal)*.7),expected:Math.round(expected),high:Math.round(principal+(expected-principal)*1.3)}});
  return <div className="projection-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="profitFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#0a7cff" stopOpacity=".28"/><stop offset="1" stopColor="#0a7cff" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} stroke="#e9ebef"/><XAxis dataKey="month" tickLine={false} axisLine={false} fontSize={11}/><YAxis tickFormatter={v=>`${Math.round(v/1000)}k`} tickLine={false} axisLine={false} fontSize={11}/><Tooltip formatter={v=>`Rs ${Number(v).toLocaleString()}`} contentStyle={{borderRadius:14,border:'1px solid #e4e7eb'}}/><Area dataKey="high" stroke="#22a865" fill="transparent" strokeDasharray="5 5"/><Area dataKey="expected" stroke="#0a7cff" fill="url(#profitFill)" strokeWidth={3}/><Area dataKey="low" stroke="#e58900" fill="transparent" strokeDasharray="5 5"/></AreaChart></ResponsiveContainer></div>
}
